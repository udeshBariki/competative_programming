```
#include<iostream>

using namespace std;

void main()
{   int n,x;
vector<int> a;

cout<<"enter the size of the array"<<endl;
cin>>n;
cout<<"enter the numbere"<<endl;
cin>>x;

cout<<"enter the array elements"<<endl;
for(int i=0;i<n;i++)
{
  cin>>a[i];
  }
int fn,cn;
int fdist=INT_MAX;
int cdist=INT_MAX;
for(i=0;i<n;i++)
{
  
  if(arr[i]>=x && cdist>(a[i]-x))
  {
  cn = i;
  cdist= a[i]-x;
  }
  if(arr[i]<=x && fdist>(x-a[i])
  {
  fn=i;
  fdist=x-a[i];
}
}
if(fdist == INT_MAX)
cout<<"floor not found"<<endl;
else
cout<<"floor is"<<a[fn]<<endl;
if(cdist == INT_MAX)
cout<<"ceil not found"<<endl;
else
cout<<"the ceil is"<<a[cn]<<endl;
}










 
 








  



